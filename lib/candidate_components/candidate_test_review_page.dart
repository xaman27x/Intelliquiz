import "package:flutter/material.dart";

class CandidateTestReviewPage extends StatelessWidget {
  const CandidateTestReviewPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
